{% extends 'layouts/base.html.twig' %}

{% block title %}Profile
{% endblock %}

{% block body %}
<div class="row">
  <div class="col-md-6 mx-auto text-center my-5">
  <h1>Profile</h1>
  <img src="{{app.user.gravatarUrl}}" alt="Avatar de {{app.user.fullName}}" class="img-thumbnail rounded-circle shadow">
  <p>{{app.user.fullName}}</p>
  <p class="">  
  <a href="{{path('app_account_edit')}}">Edit Account</a>  |
  <a href="{{path('app_account_change_password')}}">Change password</a>
  </p>
  </div>
</div>
{% endblock %}
