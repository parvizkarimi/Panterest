{% form_theme form 'bootstrap_5_layout.html.twig' %}

<div class="row">
	<div class="col-md-8 col-sm-9 col-lg-6 col-10 mx-auto">
		{{form_start(form)}}
		{{form_widget(form)}}
		<input type="submit" value="{{submitButtonText}}" formnovalidate class="btn btn-primary">
		{{form_end(form)}}
	</div>
</div>
